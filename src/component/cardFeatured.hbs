{{#with data}}
<div class="card">
                <img src="{{photo}}" alt="image" class="card_image">
                <h2 class="card_title">{{heading}}</h2>
                <p class="card_text">{{info}}</p>
                <a href="{{linkUrl}}" class="button card_button">{{linkText}}</a>
            </div>
            {{/with}}